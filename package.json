{
    "name": "couple-space",
    "version": "0.0.0",
    "private": true,
    "workspaces": [
        "server"
    ],
    "scripts": {
        "heroku-postbuild": "cd client && npm install && npm run build",
        "start": "cd server && npm start"
    }
}
